---
language: go
go:
- tip
- "1.10"
- "1.9"
- "1.8"
- "1.7"

sudo: false

go_import_path: github.com/dolmen-go/flagx

before_install:
- go list # Verify that go_import_path is ok
- go get -t -v ./...

script:
- go test -coverprofile=coverage.txt -covermode=atomic ./...

after_success:
- bash <(curl -s https://codecov.io/bash)
